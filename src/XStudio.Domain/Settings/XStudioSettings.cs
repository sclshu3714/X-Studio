namespace XStudio.Settings;

public static class XStudioSettings
{
    private const string Prefix = "XStudio";

    //Add your own setting names here. Example:
    //public const string MySetting1 = Prefix + ".MySetting1";
    // 定义一个设置项用于主题配置(示例)
    public const string ThemeSetting = Prefix + ".Theme"; // 例如："XStudio.Theme"
}
